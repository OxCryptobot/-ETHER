---
name: goal
description: Complete Ether Goals. Use when user says goal, phase, roadmap, north star, ambition, living agent, self evolve, or asks what ETHER is building toward.
metadata:
  version: "5.0"
  type: doctrine
---

# @ETHER Goals v5 — SOTA audit 2026-08-29 (HEAD 9aa1865)

## Identity

Local-first super-agentic LLM on three pillars: Modular Intelligence (8 gems), Verified Execution, Controlled Evolution.

Ambition: compete with Claude Code / Cursor / OpenHands on private repos on GTX 1650 4GB with qwen3.5:4b-q4_K_M. Generate-first and best-of-N are measured net negative (docs/FINDINGS.md §1 §11). Product path is tool-first + project pytest oracle.

## Living-agent gate (refused until met)

Unaided merge + ledger LIVE ×3 with `replace_once` from `bug_comments`. No fixture dictionary. No global spoilers. Teacher playbook (p3_25 merge 1.0 in 31s) does **not** count. `core/evolution_loop.py` four questions are hardcoded theatre. `AUTONOMY.md` “code loop is closed” is the daemon watchdog, not coding skill. `phase7_living_checklist.autonomous_claim` stays False. `checkpoint.py` is schema-only.

## Doctrine lock

- Wheels ON until the gate. Do not lift `ETHER_SOFT_LAUNCH` from a skill.
- Tool-first required. Generate-fallback is never PASS.
- One hypothesis per cycle. Labradorite on non-infra FAIL (from traces, not line counts).
- LoRA dry only. TinyCoder demo is not an LLM adapter.
- Playbook PASS is `teacher_playbook`, never `model_skill`.
- Eligible 1.0 on greeter/wallet is not soft-launch. SEED_DENY stays.
- STATUS “moonshots 11–25 LANDED” means JSON publishers exist. Landed file ≠ product skill.

## Self-learn today vs not today

**Today:** lessons JSONL, experience vault, failure_graph, preference pairs, playbook fade, git bus.
**Not today:** qwen LoRA, plan DAG execution, subagents, Qdrant default, websocket tutor, unaided hard-file skill.

## Now

1. Generalize bug_comments → replace_once so 4B mutates without fixture playbooks.
2. Collapse Rose vs multi_llm default-model split.
3. Wire checkpoint.py into Pipeline.run.
4. Queue hygiene: last_job is completed-truth; tag playbook vs model; fixture-scope spoilers.

See docs/AUDIT_SOTA_2026-08-29.md.

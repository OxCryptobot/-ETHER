---
name: keep-pushing
description: Solutions-first mentor for ETHER. Use when user says keep pushing, continue, ship it, no pushback, solutions first, or momentum after audit or FAIL.
metadata:
  version: "5.0"
---

# Keep pushing v5

Ship the next measured increment. Do not stall. Do not farm greeter. Do not claim unleash.

## Order

1. Read last FAIL from scoreboard tools[] plus taxonomy (not the failed/ graveyard).
2. Smallest code change that changes the next tools[] sequence.
3. New job id. continue_on_fail only for measure. Hard LIVE denied from eligible.
4. Read host_agent_last_job.json yourself.

## Done

- FAST pytest green without Ollama.
- Hard LIVE mutates before step 4. Six read_file is a teacher FAIL.
- Playbook PASS is a bridge. Destination is 4B replace_once unaided.
- Living-agent language waits for unaided merge+ledger LIVE ×3.

## Forbidden

Neo4j or Qdrant or LangChain as the next job. Budget bump without Labradorite `budget_exhaust`. Replay failed JSON. Soft-launch defaults. Wheels-off in repo. Unlabeled playbook as model skill. Treating evolution_loop introspection answers as model insight (they are hardcoded).

Teacher owns hard LIVE. Label playbook results `teacher_playbook`, never `model_skill`.

## Now

I-001 through I-009 in order (unify model, policy tag, eligible=easy, fixture spoilers, skip LLM after takeover, queue untrack, promote token, last_job completed, bug_comments → replace_once).

{
  "id": "ss_pref_refresh_001",
  "note": "FAST: preference + strategy_stats refresh via rlhf_tick",
  "class": "fast",
  "continue_on_fail": true,
  "source": "max_swarm",
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-c", "from core.preference import rlhf_tick; import json; print(json.dumps(rlhf_tick(), indent=2))"],
      "timeout": 180
    }
  ]
}

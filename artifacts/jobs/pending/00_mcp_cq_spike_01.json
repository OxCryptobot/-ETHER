{
  "id": "00_mcp_cq_spike_01",
  "note": "MCP Clear Quartz spike: install mcp extra, run unit tests for mcp_server + honest gate proof. Training wheels ON. One hypothesis: MCP tools call real sandbox and is_honest_tool_path_pass still rejects generate fallback.",
  "continue_on_fail": false,
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-m", "pip", "install", "-e", ".[mcp,dev]", "-q"],
      "timeout": 180
    },
    {
      "argv": [".venv/Scripts/python.exe", "-m", "pytest", "tests/test_mcp_clear_quartz.py", "-q", "--tb=line"],
      "timeout": 120
    },
    {
      "argv": [".venv/Scripts/python.exe", "-c", "from gems.clear_quartz.mcp_server import create_server, _MCP_AVAILABLE; print('mcp_available', _MCP_AVAILABLE); s=create_server() if _MCP_AVAILABLE else None; print('server_ok', s is not None)"],
      "timeout": 60
    }
  ]
}

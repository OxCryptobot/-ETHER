{
  "id": "hygiene_log_truncate_055220",
  "note": "CRITICAL hygiene: truncate artifacts/host_agent_log.txt if > 8MB to prevent GH001 push rejection",
  "continue_on_fail": true,
  "steps": [
    {
      "argv": [
        ".venv/Scripts/python.exe",
        "-c",
        "from pathlib import Path; p=Path('artifacts/host_agent_log.txt');\nif p.exists() and p.stat().st_size > 8*1024*1024:\n  data=p.read_bytes()[-2*1024*1024:]; p.write_bytes(data); print('truncated', len(data))\nelse: print('ok_size', p.stat().st_size if p.exists() else 0)"
      ],
      "timeout": 30
    }
  ]
}

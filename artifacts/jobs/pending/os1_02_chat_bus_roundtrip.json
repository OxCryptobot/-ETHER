{
  "id": "os1_02_chat_bus_roundtrip",
  "note": "OS-1: chat_bus post + receive + archive roundtrip",
  "class": "fast",
  "continue_on_fail": false,
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-c",
        "from core.chat_bus import post_operator, receive, archive, summary; env=post_operator('OS-1 smoke from host'); print('posted', env['id']); items=receive(from_grok=False, limit=5); assert any(i['id']==env['id'] for i in items), items; archive(env['id']); print('archived'); print(summary()); print('CHAT_OK')"],
      "timeout": 30
    }
  ]
}

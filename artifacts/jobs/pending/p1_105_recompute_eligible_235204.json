{
  "id": "p1_105_recompute_eligible_235204",
  "class": "measure",
  "note": "Force eligible_rates + phase1_gate recompute after controlled gate_sample LIVE samples",
  "continue_on_fail": true,
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-m", "core.measure_tick"],
      "timeout": 120
    },
    {
      "argv": [".venv/Scripts/python.exe", "-c", "from core.eligible_rates import compute; from core.phase1_gate import compute as g; print(compute()); print(g())"],
      "timeout": 90
    }
  ]
}

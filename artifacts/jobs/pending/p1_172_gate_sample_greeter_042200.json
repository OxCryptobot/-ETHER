{
  "id": "p1_172_gate_sample_greeter_042200",
  "class": "gate_sample",
  "note": "gate_sample controlled live greeter measurement budget — post-UX 0.6 rate climb (eligible fixture)",
  "continue_on_fail": true,
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-m", "scripts.batch_phase_d", "--arm", "direct", "--mode", "live", "--fixture", "greeter", "--max-steps", "10", "--timeout", "200", "--scoreboard", "artifacts/scoreboard_p1_172_greeter_live.json"],
      "timeout": 300
    }
  ]
}

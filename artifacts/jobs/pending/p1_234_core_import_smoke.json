{
  "id": "p1_234_core_import_smoke",
  "class": "fast",
  "note": "growth: import smoke AgentState/pipeline/tool_runtime/eligible",
  "steps": [
    {
      "argv": [
        ".venv/Scripts/python.exe",
        "-c",
        "from core.pipeline import Pipeline; from core.tool_runtime import TOOL_SPECS; from core.eligible_rates import compute; from core.atomic_rates import publish; print('ok', len(TOOL_SPECS), publish().get('ok'))"
      ],
      "timeout": 120
    }
  ]
}

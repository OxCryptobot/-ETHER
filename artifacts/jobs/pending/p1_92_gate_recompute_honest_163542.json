{
  "id": "p1_92_gate_recompute_honest_163542",
  "note": "1D: recompute phase1_gate + eligible_rates + measure after gate_sample live — attack honest_rate_eligible",
  "continue_on_fail": true,
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-c", "from core.phase1_gate import compute; from core.eligible_rates import compute as er; import json; print('GATE', json.dumps(compute(), indent=2)); print('ELIG', json.dumps(er(), indent=2))"],
      "timeout": 90
    },
    {
      "argv": [".venv/Scripts/python.exe", "-m", "core.measure_tick"],
      "timeout": 120
    }
  ]
}

{
  "id": "p1_96_gate_recompute_honest_224903",
  "class": "measure",
  "note": "recompute eligible rates + phase1_gate after controlled gate_sample LIVE runs (target honest_rate_eligible >=0.99)",
  "continue_on_fail": true,
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-m", "core.measure_tick"],
      "timeout": 120
    },
    {
      "argv": [".venv/Scripts/python.exe", "-c", "from core.eligible_rates import compute_and_write; print(compute_and_write())"],
      "timeout": 60
    }
  ]
}

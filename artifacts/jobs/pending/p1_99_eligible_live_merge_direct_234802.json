{
  "id": "p1_99_eligible_live_merge_direct_234802",
  "note": "Controlled LIVE measurement: hard eligible fixture merge (not in denylist) direct tool_runtime — primary attack vector for honest_rate_eligible lift",
  "continue_on_fail": true,
  "class": "live",
  "steps": [
    {
      "argv": [".venv/Scripts/python.exe", "-m", "scripts.batch_phase_d", "--arm", "direct", "--mode", "live", "--fixture", "merge", "--max-steps", "12", "--timeout", "180", "--scoreboard", "artifacts/scoreboard_p1_99_merge_live.json"],
      "timeout": 300
    }
  ]
}

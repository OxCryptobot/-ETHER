# Kernel roadmap — full + competitive + multitask

Wheels ON. Soft launch BLOCKED. Matrix RO. Exe writes.

## Phases
1 Critical — single writer, honest PASS, job schema, attach lock.
2 Architecture — ToolRuntime kernel, edit tx, critique, permissions.
3 Performance — poll cadence, targeted tests, no boot pytest on frozen.
4 Reasoning — constitution every step, loop guard, no-progress abort.
5 Autonomy — Ubuntu observe-only attach, generate cannot PASS, redact, 410 tombstone.
6 Advanced — symbol index + sandbox + targeted LIVE on 1650.
7 Experimental — moonshots after honest hard LIVE repeats.

## Multitask operations (exe owns all writes)
- attach / ollama / drain LIVE
- plan DAG node
- context retrieve
- tool step
- edit transaction
- tests-first verify
- critique on FAIL
- scoreboard honest insert
- briefing compile
- FAST drain stays Ubuntu-read of FIFO only

## Exit
Soft launch blocked until hard LIVE fixtures pass on the exe, not greeter farms.

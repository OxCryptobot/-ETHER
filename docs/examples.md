# Example prompts for @ETHER

```bash
ether plan "add JWT authentication to a FastAPI app"
ether run "write a pure function that merges two sorted lists"
ether run "write a CLI that counts lines in a file" --critique
ether run "implement binary search" --json
ether audit ./core/pipeline.py
ether index ./core
ether search "orchestrator state machine"
```

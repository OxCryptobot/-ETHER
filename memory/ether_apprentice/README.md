# ETHER Apprentice

Grok teaches; ETHER executes.

- `lessons/*.json` — craft rules + optional recovery jobs (`match` regex → `enqueue`)
- Foreman loads lessons every tick
- Dashboard shows lesson count + foreman cursor

Primary goal: transfer operational judgment from teacher (Grok) into durable host behavior so the human is not the glue.
